const Joi = require('joi');
const Model = (require('model'));
const User = Model.User;
const Work = Model.Work;

// 최신순으로 작품 목록

// 추천순으로 작품 목록

// 유저별로 작품 목록 (해당 유저가 업로드한 작품)

// 작품 아이디, 제목, 썸네일, (작품 내용), 작품 업로드한 유저의 displayName, userName

exports.recentList = (req, res) => {

};